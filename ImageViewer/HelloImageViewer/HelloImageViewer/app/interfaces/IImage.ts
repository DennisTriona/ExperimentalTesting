module app.interfaces {

    // interface to define an image with additional attributes. 
    export interface IImage {
        url: string;
        title: string;
        description: string;
    }
}